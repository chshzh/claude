# Wiki Review — 2026-05-07

Audit of `/Users/chsh/.claude/wiki` against `SCHEMA.md`.

## Summary

- Wiki path: `~/.claude/wiki`
- Pages reviewed: **16** (entities + concepts; raw + log excluded)
- Issues: **P0 = 2, P1 = 2, P2 = 1**
- Orphans: 6 | Broken links: 0 | Index gaps: 0 | Oversized (>200L): 6 | Stale (>180d): 0

## P0 Issues (must fix — auto-applied)

| Page | Issue | Fix |
|------|-------|-----|
| `concepts/deepseek-claude-code.md` | Missing required `sources:` field | Add `sources: []` + `confidence: medium` (synthesized from product docs, no raw/ file) |
| `concepts/wireguard-openwrt-china-tunnel.md` | Missing required `sources:` field | Add `sources: []` (first-hand verified config notes) |

## P1 Issues (should fix — schema clarification + acceptance)

| Page | Issue | Fix |
|------|-------|-----|
| `concepts/memfault-version-requirements.md` | `sources:` contains a URL, not a `raw/` path | Update SCHEMA.md to explicitly allow URLs in `sources:`; keep page as-is |
| `concepts/wireguard-comprehensive-guide.md` | `sources: [wireguard.com]` — bare hostname, not `raw/` path | Same — schema clarification; clean to `sources: [https://www.wireguard.com/]` |

## P2 Issues (auto-applied)

| Page | Issue | Fix |
|------|-------|-----|
| `concepts/embedded-system-general-debugging.md` | `updated: 2026-05-07v2` — invalid `YYYY-MM-DD` | Change to `2026-05-07` (drop v2 suffix; same-day edits don't need a suffix) |

## Orphan Pages (no inbound from peer concept pages — info-only)

The schema enforces ≥ 2 outbound links per page. Inbound links are advisory.
All 6 orphans are recently-added concept pages reachable from `index.md`.
No action — but consider linking these from sibling pages where topics overlap:

| Page | Suggested incoming link from |
|------|------------------------------|
| `concepts/deepseek-claude-code.md` | (none — niche) |
| `concepts/dns-over-https-doh.md` | `wireguard-openwrt-china-tunnel.md` (DNS over the tunnel) |
| `concepts/github-actions-ncs-ci.md` | `embedded-system-general-debugging.md` (CI patterns) |
| `concepts/hermes-file-map-customization.md` | `hermes-architecture.md` |
| `concepts/ttyd-web-terminal-setup.md` | `hermes-architecture.md` (web terminal access) |
| `concepts/wireguard-comprehensive-guide.md` | `wireguard-openwrt-china-tunnel.md` |

## Oversized Pages (>200 lines — split candidates, info-only)

All within reason; defer split unless they grow further.

| Page | Lines | Suggested split |
|------|-------|-----------------|
| `wireguard-comprehensive-guide.md` | 370 | Could split "embedded apps" subsection to its own page |
| `github-actions-ncs-ci.md` | 368 | Coherent — keep |
| `hermes-architecture.md` | 331 | Coherent — keep |
| `wireguard-openwrt-china-tunnel.md` | 297 | Coherent — keep |
| `svg-pptx-agent-generation.md` | 286 | Coherent — keep |
| `dns-over-https-doh.md` | 203 | Just over threshold — keep |

## Schema Improvements

`SCHEMA.md` strictly says `sources: [raw/articles/source-name.md]`. Real
practice has been mixing raw/ paths and URLs. Update SCHEMA.md to make this
explicit so the audit doesn't keep flagging legitimate uses.

Proposed addition to SCHEMA.md:
> `sources:` accepts either a `raw/...` path (preferred — content stored locally,
> deduplicated by sha256) or an external URL when the source was not saved
> locally. URLs are valid but skip drift detection.

## Applied Fixes

See following commit. Each fix is logged in `wiki/log.md`.
