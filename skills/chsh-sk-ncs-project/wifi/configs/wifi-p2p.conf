#
# Wi-Fi P2P (Wi-Fi Direct) Mode Configuration
# Use for direct device-to-device connection without infrastructure
#

# Wi-Fi driver
CONFIG_WIFI=y
CONFIG_WIFI_NRF70=y
CONFIG_WIFI_READY_LIB=y

# Wi-Fi P2P mode support
CONFIG_NRF70_P2P_MODE=y
CONFIG_NRF70_AP_MODE=y
CONFIG_WIFI_NM_WPA_SUPPLICANT=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_P2P=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_AP=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_WPS=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_EAPOL=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_INF_MON=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_GLOBAL_HEAP=y

# CRITICAL: Disable low power for stable P2P operation
CONFIG_NRF_WIFI_LOW_POWER=n

# Networking stack
CONFIG_NETWORKING=y
CONFIG_NET_SOCKETS=y
CONFIG_NET_LOG=y
CONFIG_NET_IPV4=y
CONFIG_NET_UDP=y
CONFIG_NET_TCP=y

# Network buffers
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16
CONFIG_NET_BUF_RX_COUNT=32
CONFIG_NET_BUF_TX_COUNT=32
CONFIG_NRF70_RX_NUM_BUFS=16

# CRITICAL: P2P needs more memory than Station mode
CONFIG_HEAP_MEM_POOL_SIZE=100000
CONFIG_HEAP_MEM_POOL_IGNORE_MIN=y
CONFIG_NRF_WIFI_CTRL_HEAP_SIZE=50000
CONFIG_NRF_WIFI_DATA_HEAP_SIZE=120000

# Network interface
CONFIG_NET_IF_UNICAST_IPV4_ADDR_COUNT=2
CONFIG_NET_MAX_CONTEXTS=10
CONFIG_NET_CONTEXT_SYNC_RECV=y
CONFIG_NET_L2_ETHERNET=y

# Connection Manager
CONFIG_NET_CONNECTION_MANAGER=y
CONFIG_L2_WIFI_CONNECTIVITY=y
CONFIG_L2_WIFI_CONNECTIVITY_CONNECTION_PERSISTENCE=y
CONFIG_L2_WIFI_CONNECTIVITY_AUTO_CONNECT=y
CONFIG_L2_WIFI_CONNECTIVITY_AUTO_DOWN=n
CONFIG_NET_CONNECTION_MANAGER_MONITOR_STACK_SIZE=5000

# Wi-Fi credentials storage
CONFIG_WIFI_CREDENTIALS=y
CONFIG_WIFI_CREDENTIALS_CONNECT_STORED=y
CONFIG_FLASH=y
CONFIG_FLASH_PAGE_LAYOUT=y
CONFIG_FLASH_MAP=y
CONFIG_NVS=y
CONFIG_SETTINGS=y
CONFIG_SETTINGS_NVS=y

# DHCP server for GO (Group Owner) role
CONFIG_NET_DHCPV4_SERVER=y

# DHCP client for CLI (Client) role
CONFIG_NET_DHCPV4=y

# Disable auto network init
CONFIG_NET_CONFIG_SETTINGS=n

# Network shell
CONFIG_SHELL=y
CONFIG_NET_SHELL=y

# Stack sizes
CONFIG_NET_TX_STACK_SIZE=4096
CONFIG_NET_RX_STACK_SIZE=4096

# P2P specific
CONFIG_WIFI_P2P_MAX_PEERS=5

# LTO optimization
CONFIG_LTO=y
CONFIG_ISR_TABLES_LOCAL_DECLARATION=y

# CRITICAL: P2P Timing Configuration
# These delays are essential for stable P2P connections
# - Wait for AP-STA-CONNECTED after group formation
# - Allow 4-way handshake to complete before DHCP
# - Delay DHCP client start to allow server ready
CONFIG_NET_MGMT_EVENT_QUEUE_TIMEOUT=5000
CONFIG_NET_MGMT_EVENT_QUEUE_SIZE=16

# P2P GO Intent (0-15):
# - 15: Always become GO (Group Owner)
# - 0: Always become Client
# - 1-14: Negotiated (higher = more likely GO)
# Set in Kconfig: CONFIG_P2P_GO_INTENT
