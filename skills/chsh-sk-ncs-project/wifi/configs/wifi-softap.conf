#
# Wi-Fi SoftAP Mode Configuration
# Use when device creates its own Wi-Fi network
#

# Include base Wi-Fi configuration
CONFIG_WIFI=y
CONFIG_WIFI_NRF70=y
CONFIG_WIFI_READY_LIB=y

# Networking stack
CONFIG_NETWORKING=y
CONFIG_NET_SOCKETS=y
CONFIG_NET_LOG=y
CONFIG_NET_IPV4=y
CONFIG_NET_UDP=y
CONFIG_NET_TCP=y

# Network buffers
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16
CONFIG_NET_BUF_RX_COUNT=32
CONFIG_NET_BUF_TX_COUNT=32
CONFIG_NRF70_RX_NUM_BUFS=16

# Wi-Fi memory (critical!)
CONFIG_HEAP_MEM_POOL_SIZE=80000
CONFIG_HEAP_MEM_POOL_IGNORE_MIN=y
CONFIG_NRF_WIFI_CTRL_HEAP_SIZE=40000
CONFIG_NRF_WIFI_DATA_HEAP_SIZE=100000

# Network interface
CONFIG_NET_IF_UNICAST_IPV4_ADDR_COUNT=2
CONFIG_NET_MAX_CONTEXTS=10
CONFIG_NET_CONTEXT_SYNC_RECV=y
CONFIG_NET_L2_ETHERNET=y

# SoftAP mode support
CONFIG_NRF70_AP_MODE=y
CONFIG_WIFI_NM_WPA_SUPPLICANT=y
CONFIG_WIFI_NM_WPA_SUPPLICANT_AP=y

# DHCP server to assign IPs to clients
CONFIG_NET_DHCPV4_SERVER=y

# Disable auto network init
CONFIG_NET_CONFIG_SETTINGS=n

# Network shell
CONFIG_SHELL=y
CONFIG_NET_SHELL=y

# Stack sizes
CONFIG_NET_TX_STACK_SIZE=4096
CONFIG_NET_RX_STACK_SIZE=4096

# SoftAP credentials (configure in code):
# SSID: "MySoftAP"
# Password: "mypassword123"
# IP: 192.168.1.1
# DHCP range: 192.168.1.10 - 192.168.1.100
