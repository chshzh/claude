# Skill Review — 2026-05-07

Audit of all SKILL.md files under `~/.claude/skills` against the rules in
`chsh-ag-skill-create`.

## Summary

- Skills reviewed: **28**
- Issues found: **41 raw / 17 real** (after filtering false positives)
- Severity: **P0=4 real / P1=2 real / P2=11 real**
- Skills with no issues: **10**

## P0 Issues (must fix — auto-applied)

| Skill | Issue | Fix |
|-------|-------|-----|
| `chsh-dev-ncs-project/architecture` | No YAML frontmatter — file starts with `# Architecture Patterns Reference` | Add `name` + `description` frontmatter |
| `chsh-dev-ncs-project/protocols` | Frontmatter wrapped inside ```` ```skill ```` code fence — Cursor cannot parse it; description shows as empty | Remove fence wrapper |
| `chsh-dev-ncs-project/protocols/webserver` | Same as above | Remove fence wrapper |
| `chsh-dev-ncs-project/wifi` | Same as above | Remove fence wrapper |

## P1 Issues (should fix — auto-applied)

| Skill | Issue | Fix |
|-------|-------|-----|
| `llm-wiki` | Description "Karpathy's LLM Wiki: build/query interlinked markdown KB." — missing WHEN trigger | Will be rewritten during rename to `chsh-ag-llm-wiki` |
| `chsh-dev-git-commit` | No reference to new `chsh-dev-git-release` skill (cross-ref gap) | Add to Related Skills |
| `chsh-dev-ncs-debug` | Mode G covers release flow but doesn't reference `chsh-dev-git-release` | Add to Related Skills |
| `chsh-dev-ncs-workflow` | Skill Reference table missing `chsh-dev-git-release` | Add row |
| `chsh-qa-ncs-test` | Doesn't reference `chsh-dev-git-release` for post-test release path | Add to Related Skills |

## P2 Issues (report-only — not auto-applied)

| Skill | Issue | Recommendation |
|-------|-------|----------------|
| `llm-wiki` | 506 lines > 500-line threshold | Split candidate (see below) — but the file uses progressive disclosure already; defer split unless growth continues |
| `chsh-dev-ncs-debug` | Missing Self-Update Policy section | Optional add |
| `chsh-txt-review` | Missing Self-Update Policy section | Optional add |
| `gitnexus-*` (8 skills) | Missing Self-Update Policy section | Skip — gitnexus skills are upstream-style; not part of self-update workflow |

## Split Candidates

| Skill | Lines | Status | Notes |
|-------|-------|--------|-------|
| `llm-wiki` | 506 | Borderline | Could split Obsidian sync section to a sub-skill, but content is cohesive. Defer until 600+ lines. |
| `chsh-ag-skill-create` | 495 | OK | Just under threshold |
| `chsh-dev-ncs-env` | 442 | OK | Cohesive |
| `chsh-dev-ncs-debug` | 437 | OK | Already has progressive modes A–G |

## Deduplication Candidates

| Skill A | Skill B | Overlap | Recommendation |
|---------|---------|---------|----------------|
| `chsh-dev-ncs-debug` Mode G | `chsh-dev-git-release` Steps 2–5 | Both cover CI watch + flash + verify | **Already deduped**: release skill owns the workflow, debug Mode G should defer for the release path |
| `chsh-dev-git-commit` Step 5 | `chsh-dev-git-release` Step 2 | Both run `gh run watch` | **Already deduped**: commit covers post-push CI watch; release covers full tag → release lifecycle |
| `chsh-ag-skill-create` | `~/.cursor/skills-cursor/create-skill` | 100% identical purpose | **Keep**: personal skill follows project's `chsh-` naming; Cursor's built-in is for the broader user base. No action needed. |

## Cross-Reference Gaps (auto-applied above)

The new `chsh-dev-git-release` skill is only referenced from itself and from
`chsh-ag-skill-review`. Adding cross-references to:
- `chsh-dev-git-commit`
- `chsh-dev-ncs-debug`
- `chsh-dev-ncs-workflow`
- `chsh-qa-ncs-test`

## Dead Links (false positives — no action)

All flagged dead links are inside fenced code blocks (showing example skill
structures, regex patterns, or template snippets). Not actual links to render.

- `chsh-ag-skill-create`: `reference.md`, `examples.md`, `STANDARDS.md` — example skill structure
- `chsh-ag-skill-review`: `path.md` — regex example

## Applied Fixes

See following commit. Each fix is logged in `wiki/log.md`.
